import React from 'react';

import styles from './edit-course.module.css';

const EditCourse = (): JSX.Element => {
  return (
    <section className={styles.container}>
      <h2>Welcome to edit Courses Screen</h2>
    </section>
  );
};

export default EditCourse;
