export enum StatusCodes {
  OK = 200,
  CREATED = 201,
  ACCEPTED = 202,
  NO_CONTENT = 204,
  UNAUTHORIZED = 401,
  FORBIDDEN = 403,
}

export enum ApiErrorMessages {
  TOKEN_EXPIRED = 'TOKEN_EXPIRED',
}
